-- Script Path: game:GetService("ReplicatedStorage").Client.cAudioSettings
-- Took 0.07s to decompile.
-- Executor: Velocity (1.3.6)

-- https://lua.expert/
local t = {}
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local AudioClient = require(ReplicatedStorage.Shared.AudioClient)

require(ReplicatedStorage.Shared.CVarClass)

local CVars = require(ReplicatedStorage.Shared.CVars)
local t2 = {
    [AudioClient.FADER_GAME_SOUNDS] = CVars.cl_vo_game,
    [AudioClient.FADER_GAME_MUSIC] = CVars.cl_vo_music,
    [AudioClient.FADER_MENU] = CVars.cl_menu_open
}

function t.init() --[[ init | Line: 15 | Upvalues: t2 (copy) ]]
    for v1, v2 in t2 do
        v1.Volume = v2:get()
        v2.changed:Connect(function(p1, p2) --[[ Line: 19 | Upvalues: v1 (copy) ]]
            v1.Volume = p1
        end)
    end
end

return t
