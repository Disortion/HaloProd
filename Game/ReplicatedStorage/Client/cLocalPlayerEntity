-- Script Path: game:GetService("ReplicatedStorage").Client.cLocalPlayerEntity
-- Took 0.08s to decompile.
-- Executor: Velocity (1.3.6)

-- https://lua.expert/
local ReplicatedStorage = game:GetService("ReplicatedStorage")

require(ReplicatedStorage.Components)

local World = require(ReplicatedStorage.World)

game:GetService("UserInputService")

return {
    init = function() --[[ init | Line: 10 | Upvalues: World (copy) ]]
        local function onObject(p1, p2) --[[ onObject | Line: 11 | Upvalues: World (ref) ]]
            local v1 = World.get(p1)

            if not v1.Player or v1.Player.Instance ~= game.Players.LocalPlayer then
                return
            end

            World.LocalEntityId = p1
        end

        World.ObjectCreated:Connect(onObject)
        World.ObjectModified:Connect(onObject)
    end
}
