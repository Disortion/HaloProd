-- Script Path: game:GetService("ReplicatedStorage").Client.cLightingFX
-- Took 0.07s to decompile.
-- Executor: Velocity (1.3.6)

-- https://lua.expert/
local t = {}
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local World = require(ReplicatedStorage.World)

function damageRig(p1, p2) --[[ damageRig | Line: 8 | Upvalues: World (copy) ]]
    if World.LocalEntityId then
        local isCharacterControlling = p1 == World.get(World.LocalEntityId).Player.CharacterControlling
    end
end
function t.init() --[[ init | Line: 15 | Upvalues: World (copy) ]]
    World.changed("DamageRig", damageRig)
end

return t
