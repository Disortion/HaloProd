-- Script Path: game:GetService("ReplicatedStorage").Components.MeleeHoldable
-- Took 0.07s to decompile.
-- Executor: Velocity (1.3.6)

-- https://lua.expert/
return setmetatable({}, {
    __call = function(p1, p2) --[[ __call | Line: 15 ]]
        return {
            Windup = p2.Windup or 0.25,
            Release = p2.Release or 0.25,
            Recovery = p2.Recovery or 1,
            AimPunchRecovery = p2.AimPunchRecovery or 0.1,
            AimPunchRelease = p2.AimPunchRelease or 0.1
        }
    end
})
