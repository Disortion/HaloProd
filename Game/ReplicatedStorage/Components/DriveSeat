-- Script Path: game:GetService("ReplicatedStorage").Components.DriveSeat
-- Took 0.07s to decompile.
-- Executor: Velocity (1.3.6)

-- https://lua.expert/
return setmetatable({}, {
    __call = function(p1, p2) --[[ __call | Line: 14 ]]
        return {
            SteerFloat = 0,
            MaxAngle = 40,
            WheelModel = p2
        }
    end
})
