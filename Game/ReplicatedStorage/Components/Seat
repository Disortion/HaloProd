-- Script Path: game:GetService("ReplicatedStorage").Components.Seat
-- Took 0.07s to decompile.
-- Executor: Velocity (1.3.6)

-- https://lua.expert/
return setmetatable({}, {
    __call = function(p1) --[[ __call | Line: 10 ]]
        return {
            Occupant = nil,
            SatInOffset = Vector3.new(0, 0, 0)
        }
    end
})
