-- Script Path: game:GetService("ReplicatedStorage").Components.Phone
-- Took 0.07s to decompile.
-- Executor: Velocity (1.3.6)

-- https://lua.expert/
local ReplicatedStorage = game:GetService("ReplicatedStorage")

require(script.Parent.ChatTransmitter)

local World = require(ReplicatedStorage.World)
local t = {}
local t2 = {}

function t.__call(p1, p2) --[[ __call | Line: 15 ]]
    return {
        IncomingCallReq = nil,
        CurrentCallingEntity = nil,
        OutgoingCallReq = nil,
        ContactName = p2 or "Unknown Caller"
    }
end
function t2.GetContact(p1) --[[ GetContact | Line: 25 | Upvalues: World (copy) ]]
    return ("%*"):format(World.get(p1).Phone.ContactName)
end

return setmetatable(t2, t)
