-- Script Path: game:GetService("ReplicatedStorage").Components.Containable
-- Took 0.07s to decompile.
-- Executor: Velocity (1.3.6)

-- https://lua.expert/
return setmetatable({}, {
    __call = function(p1, p2) --[[ __call | Line: 8 ]]
        if typeof(p2) == "string" then
            p2 = { p2 }
        end

        local t = {
            Types = p2
        }

        table.insert(t.Types, "any")

        return t
    end
})
