-- Script Path: game:GetService("ReplicatedStorage").Components.CarChassis
-- Took 0.07s to decompile.
-- Executor: Velocity (1.3.6)

-- https://lua.expert/
return setmetatable({}, {
    __call = function(p1, p2) --[[ __call | Line: 11 ]]
        local t = {
            VehicleMass = 0,
            RearmostAxleZ = 0,
            SteerWheels = {}
        }

        t.Acceleration = if p2 then p2.Acceleration else p2

        return t
    end
})
