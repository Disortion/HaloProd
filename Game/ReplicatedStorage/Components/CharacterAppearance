-- Script Path: game:GetService("ReplicatedStorage").Components.CharacterAppearance
-- Took 0.06s to decompile.
-- Executor: Velocity (1.3.6)

-- https://lua.expert/
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local t = {}

require(ReplicatedStorage.Shared.Types)
function t.__call(p1, p2) --[[ __call | Line: 11 ]]
    local t = {
        faceId = 1,
        hairId = 1,
        beardId = 1,
        hairColor = 1,
        sunglassesId = 1,
        suitStyle = 1,
        skinTone = 1
    }

    for v2, v3 in if p2 then p2 else {} do
        t[v2] = v3
    end

    return t
end

return setmetatable({}, t)
