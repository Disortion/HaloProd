-- Script Path: game:GetService("ReplicatedStorage").Shared.GameVariables
-- Took 0.07s to decompile.
-- Executor: Velocity (1.3.6)

-- https://lua.expert/
local t = {
    vars = {}
}

function t.set(p1, p2) --[[ set | Line: 15 | Upvalues: t (copy) ]]
    assert(if typeof(p1) == "string" then true else false)
    assert(if typeof(p2) == "number" then true else false)
    t.vars[p1] = p2
end
function t.get(p1, p2) --[[ get | Line: 25 | Upvalues: t (copy) ]]
    assert(if typeof(p1) == "string" then true else false)
    t.vars[p1] = t.vars[p1] or (if p2 then p2 else 0)

    return t.vars[p1]
end

return t
