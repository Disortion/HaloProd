-- Script Path: game:GetService("ReplicatedStorage").Shared.Backoff
-- Took 0.07s to decompile.
-- Executor: Velocity (1.3.6)

-- https://lua.expert/
local t = {}
local t2 = {}

function t.New(p1, p2, p3, p4, p5, p6) --[[ New | Line: 24 | Upvalues: t2 (copy) ]]
    local v1 = #t2 + 1

    t2[v1] = {
        Multiplier = 1,
        Base = p1 or 2,
        MaxDelay = p2 or 60,
        MinMultiplier = p3 or 0.5,
        MaxMultiplier = p4 or 10,
        FailureFactor = p5 or 1.2,
        SuccessFactor = p6 or 0.9
    }

    return v1
end
function t.Handle(p1, p2) --[[ Handle | Line: 49 | Upvalues: t2 (copy) ]]
    local v1 = t2[p1]

    if p2 ~= nil and p2 == false then
        v1.Multiplier = v1.Multiplier * v1.FailureFactor
    elseif p2 ~= nil then
        v1.Multiplier = v1.Multiplier * v1.SuccessFactor
    end

    v1.Multiplier = math.clamp(v1.Multiplier, v1.MinMultiplier, v1.MaxMultiplier)

    return math.clamp(v1.Base ^ v1.Multiplier, 0, v1.MaxDelay)
end

return t
