-- Script Path: game:GetService("ReplicatedStorage").Shared.DebugPrinter
-- Took 0.06s to decompile.
-- Executor: Velocity (1.3.6)

-- https://lua.expert/
return {
    Print = function(p1) --[[ Print | Line: 5 ]]
        local v2 = string.split(debug.info(2, "s"), ".")

        print(v2[#v2] .. "=> " .. p1 .. ("(%*"):format((debug.info(2, "l"))))
    end,
    PrintTable = function(p1) --[[ PrintTable | Line: 12 ]]
        local v2 = string.split(debug.info(2, "s"), ".")

        print(v2[#v2] .. "=> " .. ("(%*"):format((debug.info(2, "l"))))
        print(table.unpack(p1))
    end,
    ObjectToString = function(p1) --[[ ObjectToString | Line: 20 ]]
        if not p1 then
            return
        end

        local v1 = ""

        for v2, v3 in p1 do
            v1 = v1 .. "[" .. v2 .. "] "
        end

        return v1
    end
}
